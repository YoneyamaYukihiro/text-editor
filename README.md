# Multi-Server Log Viewer & Sora Editor

リモートサーバーのログ解析・編集を効率化する Python (PyQt6) 製ツールセット。

| アプリ | 役割 |
|---|---|
| **Multi-Server Log Viewer** ([ssh_log_viewer.py](ssh_log_viewer.py)) | 複数サーバーへ SSH/Telnet 同時接続、グリッドでログをリアルタイム表示 |
| **Sora Editor** ([text_editor.py](text_editor.py)) | 過去ログの編集・整形、Grep、SQL抽出/整形/実行、各種シンタックスハイライト |
| **DB Editor** ([db_editor.py](db_editor.py)) | 既存の SSH 接続プロファイルを流用し、更新系 SQL を安全装置付きで実行する DB 編集ツール (SQL実行 + グリッド編集) |

- バージョン: **1.1.8**
- 対応OS: Windows 10/11 (Linux/macOS も Python経由で動作)
- ライセンス: 内部利用

---

## 目次
1. [全体構成](#全体構成)
2. [必要環境とインストール](#必要環境とインストール)
3. [起動方法と配布](#起動方法と配布)
4. [Multi-Server Log Viewer](#multi-server-log-viewer)
5. [Sora Editor](#sora-editor)
6. [SQL抽出・整形・DB実行 (Sora Editor)](#sql抽出整形db実行-sora-editor)
7. [両アプリ共通: 設定とテーマ](#両アプリ共通-設定とテーマ)
8. [設定の移行 (Export / Import)](#設定の移行-export--import)
9. [ファイル保存場所](#ファイル保存場所)
10. [トラブルシューティング](#トラブルシューティング)
11. [キーボードショートカット早見表](#キーボードショートカット早見表)

---

## 全体構成

```
text-editor/
├── ssh_log_viewer.py        # Multi-Server Log Viewer 本体
├── text_editor.py           # Sora Editor 本体
├── app_icons.py             # アイコン生成 (PNG/ICO)
├── requirements.txt         # 依存パッケージ
├── README.md                # 本ファイル
├── assets/                  # アイコン (PNG/ICO 各サイズ)
└── dist/                    # PyInstaller ビルド出力
    ├── Multi-Server Log Viewer.exe
    ├── Sora Editor.exe
    └── README.md
```

---

## 必要環境とインストール

### 環境
- Python 3.10 以上 (推奨: 3.14 で動作確認)
- Windows 10/11

### 依存パッケージ
[requirements.txt](requirements.txt):
```
PyQt6>=6.5.0
paramiko>=3.0.0
sqlparse>=0.4.0
```

```bash
pip install -r requirements.txt
```

EXE版を使う場合は Python インストール不要。

---

## 起動方法と配布

### スクリプト実行
```bash
python ssh_log_viewer.py
python text_editor.py
python text_editor.py /path/to/file.log         # 引数でファイル開けます
python text_editor.py --search KEYWORD file.log # 起動と同時に検索
python text_editor.py --profile NAME file.log   # DB実行ダイアログで初期選択するプロファイル
```

### EXE版
[dist/](dist) フォルダ内をフォルダごと配布。受け取った人は **Python不要** でダブルクリック起動。

| ファイル | サイズ |
|---|---|
| [dist/Multi-Server Log Viewer.exe](dist/Multi-Server%20Log%20Viewer.exe) | 約 42 MB |
| [dist/Sora Editor.exe](dist/Sora%20Editor.exe) | 約 36 MB |
| [dist/README.md](dist/README.md) | 取扱説明 |

> ⚠️ **両 EXE を同じフォルダに置く**こと。Multi-Server Log Viewer の「テキストエディタで開く」機能が隣の `Sora Editor.exe` を起動するため。

### EXE 再ビルド
```bash
python app_icons.py        # アイコン (ICO/PNG) 生成

python -m PyInstaller --noconfirm --noconsole --onefile \
  --name "Multi-Server Log Viewer" \
  --icon=assets/ssh_log_viewer.ico \
  --add-data "app_icons.py;." \
  ssh_log_viewer.py

python -m PyInstaller --noconfirm --noconsole --onefile \
  --name "Sora Editor" \
  --icon=assets/text_editor.ico \
  text_editor.py
```

---

## Multi-Server Log Viewer

### 概要
最大6サーバーへ同時接続。N×M グリッドでログを並列表示し、リアルタイム追従・SQL/レベル/タイムスタンプの構文ハイライトを行えるツール。

### サーバー管理
- **SSH/Telnet 両対応**: 接続ダイアログでプロトコル選択
- **接続プロファイル**: ホスト/ユーザー/パスワード/秘密鍵/初期DIR を保存
- **初期DIRで接続**: 接続時は常に設定済みの初期DIR (`log_dir` / 📌保存値) で開く (v1.1.6で前回DIRの自動復元は廃止)
- **プロファイル管理ダイアログ**: ドラッグで並べ替え / ダブルクリックで名前変更 / 削除
- **6サーバー同時接続**: それぞれ識別色 (青/緑/橙/紫/赤/青緑) で視覚的に区別
- **ヘッダー表示**: プロファイル保存名を優先表示 (未保存なら `user@host`)、ツールチップにフル情報
- **SSH キープアライブ**: 30秒毎にパケット送信、アイドルタイムアウト切断を防止
- **自動再接続**: 通信前にトランスポート生存確認、切断検出時は1回自動再接続
- **クリーンアップ**: アプリ終了 / サーバー切断時に Tail プロセスと SSH/Telnet セッションを確実に閉じる

### ファイルブラウザ (左サイドバー)
- リモートDIRをツリー表示 (シンボリックリンク対応)
- パスバー: `⬆` 親フォルダへ / `→` 入力パスへ移動 / `↻` 再読込 / `📌` 初期DIRに保存
- **ツリーのフォルダをクリックするとパス欄に反映** (📌保存・検索対象が一致)
- **📌 初期DIR保存**: 現在のDIRを `log_dir` (初期DIR) として保存。次回接続時はこのDIRで開く
- クイックジャンプ: `📋 ログ` / `🏠 ホーム` / `🗂 /tmp`
- **右クリックメニュー**: 「ログビューアで開く」 / 「テキストエディタで開く (DL)」
- 設定で「クイックジャンプ」「説明ラベル」の表示/非表示切替

### ログ表示グリッド
- **グリッドサイズ (26プリセット)**:
  - 1×1 〜 1×6 / 2×1 〜 2×6 / 3×1 〜 3×6 / 4×3 〜 4×6
  - **5×1 / 5×2 / 5×3 / 6×1 / 6×2 / 6×3** (縦並びも対応)
- 各セルに **▶ Tail -F** (リアルタイム追従 + **ログローテーション自動追従**) / **■ 停止** / **↺ 再読込** / **✕ 閉じる**
- 停止ボタンは Tail 動作中のみ有効化
- セルごとのフィルタ (即時、デバウンス、正規表現対応)
- レベルフィルタ (ALL / ERR+ / WRN+ / INF+)
- ツールバーの**共通フィルタ**で全セル一括適用

### ツールバー (LogViewer)
グルーピング + アイコン+ラベル統一スタイルで再設計:
```
[📐 グリッド][2×2▾] │ [▶ 全Tail開始][■ 全停止] │ [🔎 共通フィルタ][_____][ALL▾][適用] │ [＋ サーバー接続] │ [💼 ワークスペース][_____][操作▾]                                              [🚨 アラート調査] │ [⚙ 設定]
```
- **緑系 (primary)**: ▶ 全Tail開始 / ＋ サーバー接続
- **赤系 (danger)**: ■ 全停止
- **右寄せ**: アラート調査・設定 (解析/管理系)
- 「設定」内に **設定移行 (Export/Import)** を統合 — めったに使わない機能はダイアログ内へ集約

### 構文ハイライト (ログ用)
- **タイムスタンプ** (`2025/06/25 13:40:20.581` 等の各種形式)
- **IPアドレス**
- **ブラケット** (`[INF]`, `[main]` 等)
- **Exception/Error** クラス名
- **スタックトレース行** (`at ...`)
- **レベル背景着色** (ERROR/WARN/INFO/DEBUG/TRACE)
- **SQL構文** (ログ内に SQL 文があれば自動着色)
- **`.sql`/`.ddl`/`.dml` 等のファイル** は全文SQLモード

### 対話的ターミナル
- サーバーパネルの **`⌨ ターミナル`** ボタンから起動
- SSH (paramiko `invoke_shell`) / Telnet (新規接続) 両対応
- コマンド送信 / `Ctrl+C` 中断 / `↑↓` 履歴ナビ / ANSIエスケープ自動除去

### ワークスペース (定型ログイン)
- 「3サーバー接続 + messvr1〜4.log 開く + グリッド3×4」のような状態を**ワンクリックで復元**
- ツールバーの「ワークスペース」コンボ + `読込 / 保存 / 管理` ボタン
- **ワークスペース管理ダイアログ**: ドラッグで並べ替え / 名前変更 / 削除
- 保存内容: グリッドサイズ + 各サーバーのプロファイル名 + 開いているログパス
- パスワードをプロファイルに保存していれば**完全自動接続**

### アラート調査 (調査支援)
- アラートメール本文を貼り付け → 主要フィールドを自動抽出:
  - **時刻 / レベル / エラーコード** (MES00059 等)
  - **ノード / プロセス / 装置**
  - **ロット / キャリア**
  - **大工程 / 小工程**
  - **概要** (本文の1行目サマリ)
- 「`小工程`」内の「`工程`」を `step_major` として誤検出する不具合を修正済み
- 接続プロファイルから一致するサーバーを推定 (host/hostname/IPベース)
- 「**調査開始**」で対象サーバーへ接続 → 該当プロセスのログを **Sora Editor で直接開く**
- プロセス名のトークン分割対応: `group02_ctlsvr1` → `ctlsvr.log` 等を自動検索
- **検索対象フォルダ**: 左ツリーの現在地 + **開いている (tail中含む) ログセルのフォルダ** (直下のみ・非再帰)
- **ローテーションログを時刻で自動特定**: ログ名の日時 (`ctlsvr1.log.20260528064257`) を解析し、
  アラート時刻 (例: 2026/05/28 16:29) を含むログを推定 → 一意に決まれば選択ダイアログを省略して直接オープン
  - 「rotation日時 >= アラート時刻のうち最小」= そのイベントを含むファイル / 全て前なら現行ログ
  - 一意に決まらない時は選択ダイアログに ★ + 自動初期選択 + 「ログ切替: 日時」併記
- 複数候補があれば選択ダイアログ表示

### テキストエディタ連携
- 左ツリーで右クリック → 「📝 テキストエディタで開く」 / ログセルの 📝 ボタン
- SSH: SFTP でバイナリ取得 / Telnet: base64 経由
- **`.gz` ファイルは自動解凍**
- 一時ファイルへ保存 → `Sora Editor` を起動
- EXE版はサイドに置いた `Sora Editor.exe` を、スクリプト版は `python text_editor.py` を起動
- **接続プロファイル名を引き継ぐ** (Sora Editor の DB実行ダイアログで自動選択)
- 一時保存先: `%TEMP%\ssh_log_viewer\<host>\<filename>`
- 起動時に 7日以上古い一時ファイルは自動削除

### ログセルから選択SQLをDB確認 (Sora連携)
- ログセル内で **SQL を含む範囲を選択 → 右クリック → 「選択範囲をSoraでSQL抽出・実行...」**
- 選択テキストを一時ファイル化 → Sora Editor を **SQL抽出ダイアログ付きで起動**
- 抽出された SQL を ▶ DB実行 でそのまま確認可能
- 接続プロファイルを引き継ぐので DB実行先も自動選択

> **Sora Editor は単一インスタンス**: LogViewer から何度開いても、毎回新しいウィンドウは立ち上がらず、**1つの Sora にタブが追加**されていきます。既に開いているファイルはそのタブをアクティブ化 (重複タブを作らない)。

### 使い方 (初回セットアップ)

1. 「**＋ サーバー接続**」 → プロトコル/ホスト/ユーザー/パスワード/ログDIR を入力
2. 「**保存**」でプロファイル化 → 「**接続**」
3. 左パネルからログを **ダブルクリック** で開く
4. `▶` ボタンで Tail-F 開始

### 使い方 (ワークスペース)

1. 上記の手順で 3サーバー接続 + ログを開いた状態を作る
2. ツールバー「ワークスペース」横の **「保存」** → 名前 (例: `日次チェック`)
3. 次回以降: コンボから `日次チェック` 選択 → **「読込」**
4. 名前変更や並べ替えは **「管理」** ボタンから

---

## Sora Editor

### 概要
マルチタブ・FTP対応のシンタックスハイライトエディタ + ログ調査ツール。Multi-Server Log Viewer から呼び出されることも想定。

### クイックアクセスツールバー
メニューを開かずに主要機能を**ワンクリック起動**:

```
[🆕 新規][📂 開く][💾 保存][📝 名前を付けて保存] | [📁 FTP] | [🔍 検索][🔎 Grep][📋 SQL抽出] | [🔖 ブックマーク]              [⚙ 設定]
```

- **ファイル操作**: 新規/開く/保存/名前を付けて保存をアイコン化
- **トグル式**: 同じボタンをもう一度押すと閉じる (FTP/検索/Grep/ブックマーク)
- **状態同期**: パネル/バーの実表示状態をボタンのチェック表示と連動
- ホバーで枠線が浮き上がる視覚フィードバック
- ツールバー下端に境界線 + タブとの間に余白 (領域が明確に分かれる)
- 右寄せの **⚙ 設定**

### マルチタブ / 単一インスタンス
- タブ ドラッグで並び替え
- タブ閉じる前に未保存編集確認
- **未保存タブはオレンジ色 + ● マーク** で強調 (保存すると通常色に戻る)
- 起動引数 `python text_editor.py <file1> <file2> ...` で開く
- **単一インスタンス動作**: 既に Sora が起動中なら、新たな起動は既存ウィンドウにタブを追加 (QLocalServer/Socket による IPC)。LogViewer から複数ファイルを開いても1ウィンドウに集約
- 既に開いているファイルを再度開こうとした場合はそのタブをアクティブ化 (重複なし)

### 名前を付けて保存
- **代表的な拡張子フィルタ** (txt / log / sql / py / js / json / csv / md / xml / html / yaml / sh / ini)
- 現在ファイルの拡張子に合うフィルタを**自動で初期選択**

### 右クリックメニュー (日本語)
エディタ上で右クリック → 編集メニューと同じ日本語表記:
- 元に戻す / やり直し / 切り取り / コピー / 貼り付け / 削除 / すべて選択
- **選択範囲からSQL抽出...** (選択あり時のみ有効)
- 各項目は状況に応じて自動で有効/無効化

### シンタックスハイライト
| 拡張子 | 言語 |
|---|---|
| `.py`, `.pyw` | Python |
| `.js`, `.ts`, `.jsx`, `.tsx`, `.mjs` | JavaScript |
| `.sql`, `.ddl`, `.dml`, `.pls`, `.plsql`, `.pck`, `.prc`, `.fnc`, `.tps`, `.tpb` | SQL |
| `.log`, `messvr.log.20260414` 等 ローテーション形式 | ログ |
| その他 | 通常テキスト |

- **ログ中のSQL自動検出**: `.log` ファイル中で `SELECT`/`INSERT INTO` 等が現れる行は **SQL 構文も色付け**
- ステータスバー右下のコンボから**手動で言語切替**可能
- 文字列リテラル内の数字は数値色で上書きされない (ルール優先順位調整済み)

### 検索 / 検索・置換
2 つを **完全分離** (Ctrl+F は検索のみ、Ctrl+H は置換欄も表示)。
- **Ctrl+F**: コンパクトな検索バー
- **Ctrl+H**: 検索 + 置換セクション (`↦` ラベルで視覚分離)
- **デバウンス + サイズ適応**: タイプ中の重い処理を回避
  - 100KB未満: 100ms / 2MB未満: 300ms / 10MB未満: 700ms / 10MB超: 1.2秒
- **ハイライト件数上限 5000件** (件数カウントは正確)
- **検索ハイライト**: 鮮黄背景 + 黒文字 + 太字 (現在行ハイライトより上に描画され、常に可視)
- **正規表現** (`.*`) / **大文字小文字** (`Aa`) のトグルボタン (押下で青く点灯)
- **検索/置換履歴の永続化**: 最大30件保存、全タブで共有 (📜 ボタン)
- **選択語を即検索**: `Ctrl+F3` で次の一致にジャンプ / `Ctrl+F` で選択語が自動入力
- **Grep との排他表示**: 一方を開くと他方は自動で閉じる

### Grep 検索
- `Ctrl+Shift+F` でフォルダ全体検索
- **コンパクトな2行UI**: 上段=フォルダ / 下段=パターン+globフィルタ+オプション+検索ボタン
- 緑強調の **▶ 検索** ボタン (主アクション)
- `Aa` / `.*` トグルで大小区別/正規表現切替
- `*.py, *.log` のような複数globパターン対応
- **マッチ箇所を黄色背景でハイライト** (QStyledItemDelegate でカスタム描画)
- **各結果行にファイル名 (basename)** を併記 (ヘッダーがスクロールアウトしてもどのファイルか分かる)
- **長い行はマッチ周辺だけ表示**: 200文字超のログ行はマッチ前後の30/200文字に切り詰め + `…`
- ホバーで完全な行をツールチップ表示
- **ジャンプは複数手段対応**: ダブルクリック / Enter / シングルクリック
- **ファイル追記によるズレを自動補正**: マッチ行テキストを記憶し、行番号がズレた場合はテキスト一致で実位置を探索
- 結果ツリーから該当行へジャンプ → エディタが自動スクロール + フォーカス取得
- **✕ ボタン / Esc キー** で閉じる

### ブックマーク
- `F2` で **登録/解除** (トグル) / `F3` / `Shift+F3` で次/前ブックマーク
- `Ctrl+B` で一覧パネル (`🔖 ブックマーク一覧`)
- 一覧の各行は **行内容を200文字までプレビュー** + ホバーで全文ツールチップ
- ブックマークが空の時は一覧に**使い方ヒント**を表示
- **✕ ボタン / Esc キー** で閉じる
- ジャンプはダブルクリック / Enter
- ガター左端の **青い◆** が登録行の目印

### 編集箇所ナビゲーション (VS Code 風)
- ガター (左の縦バー) で**追加/変更/削除** 行を色分け表示
  - 緑バー = 追加 / 橙バー = 変更 / 赤バー = 削除
- **行背景も淡く着色** (より目立つ)
- `Alt+↓` / `Alt+↑` で次/前の編集箇所へジャンプ

### 現在行ハイライト (黄色統一)
- **現在行に薄い黄色オーバーレイ** (検索の黄色マッチが透過して見える)
- 左端に**黄色アクセントライン** (幅2px)
- ガター内の**行番号も黄色 + 太字**で強調
- ガター背景にも薄い黄色帯
- → カーソル位置が一目瞭然、かつ検索ヒットも隠さない

### エラー行ナビゲーション
- `Ctrl+E` / `Ctrl+Shift+E` で ERROR/WARN/EXCEPTION を含む行へジャンプ

### 行へジャンプ
- `Ctrl+G` で行番号入力 → センタリング表示
- **折り返し行があっても正確に論理行へジャンプ** (`findBlockByNumber` 使用)

### 全て保存 / 全て保存して終了
- `Ctrl+Alt+S`: 変更ありの全タブを一括保存 (未指定タブは「名前を付けて保存」)
- `Ctrl+Alt+Q`: 全保存 → アプリ終了 (キャンセル時は終了しない)

### 外部編集検知 (VS Code 風)
- `QFileSystemWatcher` で開いている全ファイルを監視
- **未編集タブ**: 外部で変更されたら**自動でリロード** (ステータスバー通知)
- **編集中タブ**: 「リロードしますか？」ダイアログ (デフォルトNo)
- 自分が保存した直後の自己トリガーは1秒間無視
- **Grep ジャンプ時にも自動リロード**: ディスクとタブ内容を比較、差分があれば更新

### FTP接続
- ツールバー **📁 FTP** ボタンまたは `Ctrl+T` で FTPパネル表示
- ホスト/ユーザー/パスワード入力 → プロファイル保存可
- **プロファイル管理** (LogViewer と同操作): 接続ダイアログの「管理」ボタンから
  - ドラッグで並べ替え / ダブルクリックで名前変更 / 🗑 削除
  - (削除は管理ダイアログに集約。接続ダイアログのボタンは「保存」「管理」の2つ)
- **接続表示にプロファイル名優先** (明るい緑 #9ED969 太字)
- **ファイル一覧は2行表示** (v1.1.6): 1行目=名前 (幅が足りなければ … 省略)、2行目=**サイズ・更新日時**を淡色小フォント。ホバーで全文+メタをツールチップ表示 (フォルダはサイズ無しで1行)
- **last_dir 自動記憶**: 切断時のディレクトリを次回接続時に自動復元
- **キープアライブ + 自動再接続**: 60秒毎に NOOP を送りアイドル切断を防止。
  万一切れても操作時に自動再接続 (切断前の DIR を復元)
- アプリ終了時に QUIT 送信 + タイマー停止 + last_dir 保存を確実に実行
- **複数選択ダウンロード**: Ctrl/Shift+クリックで複数選択 → ボタンに件数表示 → 順次DL
- **サイズ事前警告**: 5MB超で確認 / 100MB超で強警告
- **進捗ダイアログ** (大ファイル対応):
  - ダウンロード済 / 合計バイト数 / 進捗率
  - **転送速度** (例: `2.4 MB/s`)
  - **残り時間** (例: `残り 7 秒`)
  - **キャンセルボタン** で安全に中断 (一時ファイル削除)
- **ストリームDL**: メモリではなく一時ファイルへ直接書き込み (大ファイル対応)
- 一時保存先: `%TEMP%\text_editor_ftp\<host>\<filename>`
- 同名ファイル再DL時:
  - 未編集タブ → 自動でタブをリロード
  - 編集中タブ → 上書き確認ダイアログ
- 起動時に 7日以上古い一時ファイルは自動削除

---

## SQL抽出・整形・DB実行 (Sora Editor)

ログから SQL を取り出し、整形し、必要なら **そのまま実行して結果を取得** できる一連の機能。アラート受信 → ログ調査 → DB結果検証 のフローを1ツールで完結。

> 💡 **SQL抽出ダイアログも DB実行ダイアログもモードレス** で動作します。開いたままメイン画面で別タブを編集したり、他のファイルを開いたりできます。DB実行ダイアログを2回呼ぶと、既存ダイアログに SQL を差し替えて前面化する仕様 (重複起動防止)。

### SQL抽出・整形
3通りの呼び出し:

| 操作 | 範囲 |
|---|---|
| **Ctrl+Shift+Q** / ツールバー 📋 | タブ全体から抽出 |
| **Ctrl+Shift+X** / ツールメニュー | 選択範囲のみから抽出 |
| **右クリック → 選択範囲からSQL抽出** | 選択範囲のみから抽出 |

- ログ中の SQL 文 (Hibernate/Execute SQL/etc.) を自動抽出
- 種別バッジ表示 (`[SEL]` / `[INS]` / `[UPD]` / `[DEL]`)
- 種別ごとの色 (明るい緑/青/橙/赤) で一覧から識別容易
- **絞り込みフィルタ**: キーワード入力 + 種別 (全種別/SELECT/INSERT/UPDATE/DELETE) で大量SQLから即座に該当を発見
  - `234 / 733 件 (絞り込み中)` のように件数表示
- 整形オプション: キーワードケース (upper/lower/cap) / インデント幅 / バインドパラメータ置換
- **INSERT を列=値で表示** トグル (デフォルトON):
  - ON: `列名 = 値` の対応表で表示 (カラムが多くてもどの列に何が入るか一目)
  - OFF: 通常のSQL文で表示 (コピーして実行・流用したい時)
  - カッコ/クォート内のカンマを考慮した賢い分割で `TO_DATE('a','b')` 等も正しく対応
- 整形プレビューにも SQL シンタックスハイライト
- **▶ DB実行** ボタン (緑) でそのまま DB へ投入可
- 全件1タブで開く / コピー / 個別をタブで開く

> 長いログから「この10行分のSQLだけ実行したい」場合: **範囲選択 → 右クリック → 選択範囲からSQL抽出 → ▶DB実行** の流れが最短。

### DB実行ダイアログ (▶ DB実行)
SSH 経由でリモートサーバーのシェル上から `sqlplus` / `mysql` / `psql` 等を実行し、結果を取得。

#### 接続
- 接続プロファイルは `.ssh_log_viewer_profiles.json` を共有
- 起動元 (LogViewer) から `--profile` で渡されたプロファイルが**自動選択**
- **DB実行コマンド設定欄**は折りたたみ可能 (`▶ DB実行コマンド設定`)。通常運用ではプロファイルに保存された値で実行するため見えなくてOK

#### DBプリセット
- **Oracle (sqlplus)** — CSV 出力に統一 (表示モードは結果欄の4モードで切替)
- **MySQL/MariaDB / PostgreSQL / SQLite / SQL Server (sqlcmd)**

> v1.1.0 で3つあった Oracle プリセット (通常/CSV/縦) は **CSV 一択** に統合。
> sqlplus 出力は CSV にしておき、見せ方は結果表示モード (横/縦転置/1件詳細) で切替する設計に。

#### 自動安全弁 (実行時に自動補正)
- **`<<EOF` → `<<'EOF'`**: bash 変数展開を防止 (`M_CONDITION$RECIPE` のような `$` 入りオブジェクト名を保護)
- **`set sqlblanklines on define off` 自動挿入**: SQL中の空行で文が切れる/`&` 変数の対話プロンプトを防止
- **`set markup csv on quote off` → `quote on`**: 値内のカンマで CSV パースが崩れるのを防止
- **SQL中の空行を自動除去**
- **改行コードを LF に正規化** (CRLF 起因の heredoc 終端不一致を防止)

#### 実行方式 (3種)
| 方式 | 用途 |
|---|---|
| **SFTP一時スクリプト + bash -l (推奨)** | `/tmp` にスクリプト書き込み → `bash --login` 実行 → 自動削除。csh/tcsh 環境でも確実 |
| **bash -l + stdin** | `bash -l` を起動し stdin 経由でコマンド送信 |
| **デフォルトシェルに直接** | リモートシェルにそのまま投げる |

#### 安全機構 (実行ブロック)
読み取り系のみ許可:
- **許可**: `SELECT / WITH / EXPLAIN / DESC / DESCRIBE / SHOW`
- **ブロック**: `INSERT / UPDATE / DELETE / MERGE / TRUNCATE / DROP / CREATE / ALTER / GRANT / REVOKE / COMMIT / ROLLBACK / SAVEPOINT / LOCK / CALL / EXEC` 等
- **`FOR UPDATE` / `FOR SHARE` 等の行ロック取得もブロック** (文字列/コメント内の偽陽性を避けるため、リテラル除去後に検出)
- 先頭コメント (`-- ...` や `/* ... */`) を除去してから先頭キーワード判定

#### 結果表示 (4 モード切替)
| モード | 内容 | 用途 |
|---|---|---|
| **📊 全件 (横・グリッド)** | 通常のテーブル | 列が少ない / 横並び比較 |
| **📋 全件 (縦・転置)** | 列名を左ヘッダー、各レコードを横列 (`#1, #2, ...`) | 列が多い + 全件一覧 |
| **🔍 1件詳細 (縦)** | 1レコードのみ縦表示 + `<< < > >>` で巡回 | 列が多い + 1件ずつ確認 |
| テキスト | 生の出力 | デバッグ |

- ヘッダーは全モードで **黄色 (#9ED969) + 太字** で統一
- **読み取り専用** (ダブルクリックでも編集不可)
- **行選択モード** (Excel貼付しやすい)
- 列幅は内容自動調整 (上限400px)

#### 出力後処理
- **「結果のみ表示」** (デフォルトON): sqlplus banner / `SQL>` プロンプト / 切断メッセージを自動除去
- **「送信コマンドも結果に表示」**: デバッグ用に実行内容を表示
- **「ドライラン」**: 実行せず生成コマンドだけ表示
- **0件警告**: 結果が空の時は ⚠ オレンジ太字でステータスバー / グリッドに通知
- **件数表示**: ステータスバー右下に `· N 件`

#### 上限件数 (UI 凍結防止)
コンボから選択: `100 / 500 / 1000 (デフォルト) / 5000 / 10000 / 無制限`
- 超過時は表示を切り詰め + ⚠ 警告表示 (SQL 自体は全件実行)
- 全件確認したい場合は SQL に `ROWNUM ≤ 1000` / `FETCH FIRST 1000 ROWS ONLY` を付与推奨

#### SQL ナビゲーション
SQL抽出ダイアログから呼ばれた場合は、DB実行ダイアログ内でも抽出済みSQLを**順次切替可**:
```
[<<] [<] [ 5 ] [/ 24] [>] [>>]
```
- 番号欄に数値入力 → Enter で任意の SQL にジャンプ
- 切替時は結果欄が自動クリア
- 親 (SQL抽出ダイアログ) の選択行も同期

#### 実行
- メインの **▶ 実行 (Ctrl+Enter)** ボタンは緑グラデーションで目立つ配色
- 状態表示: 実行中 → 完了 (exit=0): プロファイル名 · 24件

### CSV出力時の特別対応
- `set markup csv on quote on` を自動適用
- `csv.reader` でクォート付き CSV を正しくパース
- **値内に改行を含む CSV** (Oracle の複数行 COMMENTS 列等) も正しく解釈

---

## DB Editor — SSH経由DB編集ツール

[db_editor.py](db_editor.py) は、Sora Editor の `DBExecuteDialog` が **参照専用**
(SELECT/WITH 等のみ) なのに対し、**更新系 SQL (INSERT/UPDATE/DELETE/MERGE/DDL)**
を実行できる独立アプリです。

```bash
python db_editor.py
```

### 設計方針

- **既存の SSH 接続を流用**: 接続層は `ssh_log_viewer.SSHConnection`、接続プロファイルは
  両アプリ共通の `~/.ssh_log_viewer_profiles.json` をそのまま使う (再設定不要)
- **シンタックスハイライト/テーマ**も Sora Editor のものを流用
- **トランザクション方式 (案A: ステージング + 一括TX)**: 編集 SQL は 1 回の
  `exec_command` でまとめて送信し、テンプレート側で `BEGIN ... COMMIT` /
  失敗時 `ROLLBACK` を保証する

### 安全装置

| 装置 | 内容 |
|---|---|
| プロファイル別 編集許可 | 「このプロファイルで編集を許可」を ON にしない限り更新系 SQL をブロック (本番誤実行防止) |
| WHERE 句必須チェック | WHERE のない UPDATE/DELETE を警告 |
| 危険操作の警告 | DROP / TRUNCATE / ALTER 等を検出して警告表示 |
| 影響行プレビュー | UPDATE/DELETE の WHERE 条件を `SELECT COUNT(*)` に変換して件数確認 |
| ドライラン | 実行せず、生成スクリプト全文を表示 |
| 実行前の最終確認 | 更新系 SQL は内容・注意点を提示して明示確認 |
| 監査ログ | 実行した編集系 SQL を `~/.db_editor_audit.log` に追記 |

### DBプリセット (編集系テンプレート)

Oracle (sqlplus) / MySQL・MariaDB / PostgreSQL / SQLite / SQL Server (sqlcmd) の
雛形を内蔵。いずれもトランザクションで包み、エラー時に ROLLBACK される構成。
`USER/PASS/SID` 等を実値に書き換え、プロファイルへ保存できます。

### タブ構成

1. **SQL 実行** — 更新系 SQL を直接実行
2. **グリッド編集** — SELECT 結果を表で取得 → セルを直接編集 → 主キーから UPDATE
   を自動生成 → ステージング → 一括コミット

#### グリッド編集の使い方

1. テーブル名・主キー列 (複数可、`,` 区切り) と SELECT 文を入力し **データ取得**
   - 取得は参照用テンプレート (CSV 出力) で実行。プロファイルの `db_cmd` を流用
2. 表のセルをダブルクリックして編集 (変更セルは色付きでステージング表示)
3. **変更をプレビュー** で生成される UPDATE 文を確認 (WHERE は取得時点の主キー値で行を特定)
4. **変更を適用** で確認ダイアログ → 編集用テンプレート (BEGIN..COMMIT) で 1 トランザクション送信
   - 空欄は既定で `NULL`、`NULL` トークンは NULL、値は `'...'` でエスケープ (`''`)
   - 適用成功でセル値が新しい基準値に確定し、ステージングはクリアされる

---

## 両アプリ共通: 設定とテーマ

両アプリは同じ設定パターンで個別に保存されます。

### 設定ダイアログ
- Multi-Server Log Viewer: ツールバーの **`⚙ 設定`**
- Sora Editor: ツールバーの **`⚙ 設定`** または ファイルメニュー → **`⚙ 設定...`** (`Ctrl+,`)

### フォントサイズ
- ssh_log_viewer: 左サイドバー / ツールバー / ログセル本文 / ターミナル (各 7〜24px)
- text_editor: エディタ本文 / UI (タブ・パネル) (各 8〜28px)

### テーマプリセット (6種、両アプリ共通)
| 名前 | 種別 | 特徴 |
|---|---|---|
| Dark | 暗 | デフォルト |
| Dark Blue | 暗 | 紺色アクセント |
| High Contrast | 暗 | 純黒背景・白文字 |
| Monochrome | 暗 | グレースケール |
| **Light** | 明 | 白背景・黒文字 |
| **Solarized Light** | 明 | 目に優しい配色 |

### テーマ連動箇所
- ウィンドウ全体 (背景・テキスト・選択色・スクロールバー・ツールチップ)
- 各種ボタン・コンボ・スピン・ライン入力
- エディタ本体 (背景・文字色・選択色)
- 行番号ガター
- 現在行ハイライト (黄色オーバーレイ統一)
- 検索ハイライト (黄色)
- ログレベル背景色 (テーマ別に最適化)
- SQL/タイムスタンプ等の構文色 (ダーク/ライト別に切替)
- サーバー識別色パレット (ssh_log_viewer)

### スクロールバー
- 幅 12px / 角丸ハンドル / ホバー時色変化

---

## 設定の移行 (Export / Import)

別PCへ作業環境を移したいとき、設定を **JSON 1ファイル** にまとめて持ち運べます。

v1.1.1 から **設定ダイアログ内に集約**: めったに使わない機能なのでツールバー/ファイルメニュー直下から外し、`⚙ 設定` → 「設定移行」セクションへ移動。

### 両アプリ共通の入口

1. ツールバー (またはファイルメニュー) の **`⚙ 設定`** を開く
2. ダイアログの下部「**設定移行 (PC移行・バックアップ)**」セクション
3. **📤 エクスポート (JSON保存)** / **📥 インポート (JSON読込)** ボタン

### エクスポートに含まれる内容
| アプリ | 含まれるもの |
|---|---|
| Multi-Server Log Viewer | 接続プロファイル (log_dir/db_exec_cmd 含む) / ワークスペース / UI設定 |
| Sora Editor | FTPプロファイル (last_dir 含む) / UI設定 / 検索履歴 |

### 動作
インポート時に3択ダイアログ:

| 選択 | 動作 |
|---|---|
| **Yes (マージ)** | 既存設定を残しつつ、同名は新しい方で上書き |
| **No (置換)** | 既存を全消去して入れ替え |
| **Cancel** | 中止 |

### PC移行手順
1. **旧PC**: `⚙ 設定` → 📤 エクスポート → 任意の場所に JSON 保存 → USB/クラウド等で運搬
2. **新PC**: アプリ起動 → `⚙ 設定` → 📥 インポート → JSON 選択 → マージ/置換選択

これでサーバープロファイル/ワークスペース/設定/履歴が **全部一括移行** できます。

> ⚠️ パスワードも JSON に平文で含まれます。USB等で運搬する際は紛失に注意。

---

## ファイル保存場所

すべて `C:\Users\<username>\` 直下:

| ファイル | 内容 |
|---|---|
| `.ssh_log_viewer_profiles.json` | サーバー接続プロファイル (log_dir / db_exec_cmd 等含む) |
| `.ssh_log_viewer_settings.json` | フォントサイズ・テーマ・左パネル表示 |
| `.ssh_log_viewer_workspaces.json` | ワークスペース定義 |
| `.text_editor_ftp_profiles.json` | FTP接続プロファイル (last_dir 含む) |
| `.text_editor_settings.json` | エディタ設定 (フォント/テーマ) |
| `.text_editor_history.json` | 検索/置換履歴 |

### 一時ファイル
| 場所 | 内容 | 自動削除 |
|---|---|---|
| `%TEMP%\ssh_log_viewer\<host>\` | 「テキストエディタで開く」でDLしたログ | 7日以上前は起動時に削除 |
| `%TEMP%\text_editor_ftp\<host>\` | FTPでDLしたファイル | 同上 |
| `/tmp/sora_db_exec_*.sh` (リモート) | DB実行用一時スクリプト | 実行完了後に自動削除 |

> ⚠️ **パスワードは平文保存**。共有PCで使う場合は注意してください。

---

## トラブルシューティング

### 接続関連
| 症状 | 対処 |
|---|---|
| SSH/Telnet 接続できない | ホスト/ポート/FW確認、`ssh host` で個別確認 |
| Telnet で応答なし | サーバー認証プロンプトの表記 (`login:`/`Username:`等) を要確認 |
| `Socket is closed` エラー | アイドルタイムアウト切断。**再接続すれば keepalive 有効になり次回以降は防げます** |
| パスワード入力プロンプト | プロファイルにパスワード未保存 |
| ターミナル入力が反映しない | エディタ側の入力欄にフォーカスを置く |

### Tail-F 関連
| 症状 | 対処 |
|---|---|
| Tail が止まる | `-F` なので通常自動復帰。`▶` ボタンを OFF→ON で再開 |
| ローテーション後新ファイルに飛ばない | `tail -F` が正しく動いていない場合がある (古いOSなど) |

### DB実行関連
| 症状 | 対処 |
|---|---|
| `ORA-12154: TNS:could not resolve...` | テンプレートの `USER/PASS@SID` 部分を実値に書き換え |
| 結果が空 (出力ゼロ) | `feedback off pagesize 0` で 0 件が完全無出力に。CSV プリセットなら「結果のみ表示」ON で `⚠ 0 件` が出る |
| sqlplus banner が結果欄に出る | 「結果のみ表示」を ON |
| 値内のカンマで列がズレる | 自動補正で `quote on` に切り替わる。テンプレートを保存し直すと永続化 |
| 日本語条件で 0 件 | サーバーの NLS_LANG が Shift_JIS 系の場合、UTF-8 で書き込んだ SQL とミスマッチする可能性 |
| FOR UPDATE がブロックされる | 仕様。本ツールは純粋な参照のみ許可 |

### 表示関連
| 症状 | 対処 |
|---|---|
| 文字化け | サーバー側のロケールが UTF-8 か確認 |
| 検索が遅い | 自動でデバウンス、超巨大ファイルでは Enter のみで検索 |
| Light テーマで一部が暗いまま | 設定で再選択して OK を押す → 全UI に再適用される |
| Grep ジャンプが対象行に行かない | 折り返し行による論理行/視覚行の不一致は v1.1 で修正済み (`findBlockByNumber`) |

### EXE関連
| 症状 | 対処 |
|---|---|
| 初回起動が遅い | PyInstaller の解凍。2回目以降は速い |
| ウイルス対策ソフトに止められる | PyInstaller製は誤検知されやすい。除外設定追加 |
| Windows SmartScreen が出る | 署名なしEXEのため。「詳細情報」→「実行」で回避 |

---

## キーボードショートカット早見表

### Multi-Server Log Viewer
| 操作 | 場所 / 機能 |
|---|---|
| ツールバー | グリッドサイズ変更・全Tail (▶)・全停止 (■)・共通フィルタ・ワークスペース・アラート調査・設定 (設定移行は設定ダイアログ内) |
| サーバーパネル「⌨」 | 対話的ターミナル起動 |
| パスバー `↻` | DIRツリー再読込 |
| パスバー `📌` | 現在DIRをプロファイルに保存 |
| ログセル `▶ / ■ / ↺ / ✕` | Tail開始/停止/再読込/閉じる |

### Sora Editor
| キー | 動作 |
|---|---|
| Ctrl+N / O / S / Shift+S | 新規 / 開く / 保存 / 名前付き保存 |
| **Ctrl+Alt+S** | **全て保存** |
| **Ctrl+Alt+Q** | **全保存 → 終了** |
| Ctrl+Q | 終了 |
| Ctrl+F | 検索 (置換欄非表示、選択中なら自動入力) |
| **Ctrl+H** | **検索・置換 (置換欄表示)** |
| **Ctrl+F3** | **選択語を即検索** |
| Ctrl+Shift+F | Grep |
| **Ctrl+G** | **行へジャンプ** |
| **Alt+↓ / Alt+↑** | **次/前の編集箇所** |
| F2 / F3 / Shift+F3 | ブックマーク 登録/解除 / 次へ / 前へ |
| Ctrl+E / Ctrl+Shift+E | 次/前エラー行 |
| Ctrl+B | ブックマーク一覧 |
| Ctrl+T | FTPパネル |
| Ctrl+Shift+Q | SQL抽出 (タブ全体) |
| **Ctrl+Shift+X** | **選択範囲からSQL抽出** |
| **Ctrl+,** | **設定** |
| **Ctrl+Enter** | **DB実行ダイアログで実行** |
| Esc | Grep/ブックマーク/検索バーを閉じる |

> Ctrl+Z/Y/X/C/V (元に戻す/やり直し/切取/コピー/貼付) はエディタ内蔵処理。
> メニューにショートカットを重複設定すると「ambiguous shortcut」で発火しなく
> なるため、これらはエディタネイティブに委ねている (メニューはクリック用)。

### DB実行ダイアログ
| キー | 動作 |
|---|---|
| Ctrl+Enter / Ctrl+Return | ▶ 実行 |
| Ctrl+Home / End | SQLリスト 先頭/末尾 へ |
| Ctrl+← / → | 前/次の SQL へ |

---

## 開発履歴

| バージョン | 主な変更 |
|---|---|
| **1.1.8** | **メモリリーク修正(LogViewer)**: tail を長時間放置するとログセルのメモリが無限に増え続ける問題を修正。表示(QPlainTextEdit)に `setMaximumBlockCount`、元データ `_all_lines` に保持上限(`_MAX_LINES=50000`)を導入し、古い行を自動破棄してメモリを一定に保つ。 |
| **1.1.7** | **テーマ追従の全面強化(両アプリ)**: ライト系テーマで暗色固定だった箇所を一掃 — Sora はツールバー/検索バー/Grep/ブックマーク/SQL抽出・SQL実行ダイアログ(SQL入力欄・結果グリッド含む)/FTPパネルをテーマ連動に。**シンタックス配色をテーマ明暗で切替**(ライト時は高コントラストな濃色)。LogViewer はツールバー(自前スタイルをテーマ生成メソッド化)/パスバー/右クリックメニュー/サーバー接続ダイアログをテーマ連動にし、**テーマ変更が再起動なしでライブ反映**(ツールバー自前スタイルの再適用 + 全ウィジェット強制再ポリッシュ)。**ログ→ソースジャンプ(Sora)**: ログの `Class#method():行` / Javaスタック / `File.ext:行` / C++ `Class::method()`(行番号なしはメソッド定義を検索)を解析し、登録した「ソース検索パス」から該当ソースを開いて該当行へジャンプ(複数候補は選択、全て開く可、ジャンプ先を自動ブックマーク)。C/C++・Java・C#・XML/HTML のシンタックスハイライト追加。**ブックマーク**: 右サイドバーへ移動、ファイル単位でJSON永続化(`~/.text_editor_bookmarks.json`)、ヘッダ重複表示の修正。**FTPパネル2行表示**(名前 + サイズ・更新日時)。検索履歴ポップアップのチラつき修正。設定ダイアログをタブ化(両アプリ)。エディタフォントをpx指定に統一(段差を均一化)。接続ボタンを緑で統一。LogViewer の前回DIR自動復元を廃止し初期DIR(log_dir)で接続。 |
| **1.1.6** | **SQL抽出の誤検出を修正** (Sora): `[日時][レベル][スレッド][クラス#method():行]` 形式のログで、本文末尾の `#delete`/`#insert` 等が SQL キーワードに誤マッチし**ログ全体を1つのSQLとして整形**してしまう問題を修正。本文の**先頭**がSQL文で始まる場合のみ抽出 (アンカー判定)、ブラケット日時形式のログ行認識、裸レベル語/ブラケット連鎖の除去を追加。**FTPパネルを2行表示に** (Sora): 1行目=名前 (…省略)、2行目=**サイズ・更新日時**を淡色小フォント、ホバーで全文+メタ。**接続ボタンを緑(プライマリ)で両アプリ統一**。**LogViewer の前回DIR自動復元 (last_dir) を廃止**: 接続時は設定済みの初期DIR (`log_dir` / 📌保存値) を使用。Sora は初期値設定が無いため**前回DIR保存を維持**。 |
| **1.1.5** | アラート調査の強化: 検索対象に**開いている (tail中) ログセルのフォルダ**を追加 (サブフォルダ配置のログも特定可)、**ローテーションログをアラート時刻で自動特定** (`ctlsvr1.log.YYYYMMDDHHMMSS` を解析し該当時刻のログを推定→一意なら直接オープン)。LogViewer に **⬆ 親フォルダボタン** + ツリーのフォルダクリックでパス欄同期。**📌 初期DIR保存** を `log_dir`+`last_dir` 両方に保存して確実に反映。**全停止ボタンをはっきりした赤**に。「アラート解析」→「アラート調査」に名称統一。 |
| **1.1.4** | INSERT文を**列=値の対応表**で整形 (カラム多数でも対応が一目、ON/OFFトグルで生SQL表示にも切替可、カッコ/クォート内カンマを考慮した分割)。ブックマーク一覧のプレビューを200文字に拡大 + 全文ツールチップ、空時に使い方ヒント表示。ブックマークのラベルを「マーク切替」→「登録/解除」に変更 (動作が直感的に)。 |
| **1.1.3** | **Sora Editor を単一インスタンス化** (QLocalServer/Socket): 何度起動しても1ウィンドウにタブ追加、既開ファイルはタブをアクティブ化。**LogViewer のログセルから選択SQLをSora連携**: ログセルで範囲選択 → 右クリック → 「選択範囲をSoraでSQL抽出・実行」→ Sora が SQL抽出ダイアログ付きで起動 (`--sql-extract` フラグ)。Sora起動ロジックを共通ヘルパー化。 |
| **1.1.2** | Sora Editor 操作性強化。**選択範囲からSQL抽出** (Ctrl+Shift+X / 右クリック / メニュー) — 長いログから部分的に抽出可能。SQL抽出ダイアログに**絞り込みフィルタ** (キーワード + 種別)。ツールバーに**ファイル操作** (新規/開く/保存/名前を付けて保存) を追加。名前を付けて保存に**代表拡張子フィルタ** + 自動初期選択。未保存タブを**オレンジ強調**。右クリックメニューを**日本語化** (編集メニューと統一)。**FTPキープアライブ + 自動再接続** (アイドル切断防止) + アプリ終了時の正常切断。**FTPプロファイル管理ダイアログ** (ドラッグ並替/改名/削除、LogViewer と同操作) + 接続ダイアログから削除ボタン撤去。ツールバー/タブ境界に区切り線・余白。Undo/Redo 等のショートカット競合 (ambiguous shortcut) を解消。 |
| **1.1.1** | UI/UX 改善リリース。LogViewer ツールバー再設計 (グルーピング + アイコン+ラベル統一 + 緑/赤色分け)。グリッドプリセット 26 種に拡張 (×5/×6 + 5×N/6×N 追加)。アラート調査の抽出フィールド拡張 (装置/キャリア/エラーコード/概要、`小工程`内`工程`誤検出修正)。DBプリセットを Oracle 3種 → CSV 一択に統合 (表示モードで網羅)。**SQL抽出 / DB実行ダイアログをモードレス化** (メイン画面と並行操作可能、GC対策・重複起動防止)。設定移行 (Export/Import) を `⚙ 設定` ダイアログ内に集約 (両アプリ、ツールバー/メニュー直下からは削除)。 |
| **1.1.0** | DB実行ダイアログ追加 (SSH経由で sqlplus/mysql/psql 等を実行、4種類の結果表示モード、読み取り専用ブロック、FOR UPDATE 検出、CSV自動補正、上限件数、SQL ナビゲーション)。SQL抽出に種別バッジ + DB実行ボタン。クイックアクセスツールバー (トグル式)。検索/Grep UI 刷新 (アイコン化、トグルボタン、緑強調)。検索・置換セパレート (Ctrl+F vs Ctrl+H)。Grep マッチ箇所黄色ハイライト + ファイル名併記 + 長行切詰。現在行を黄色オーバーレイ統一 (検索ハイライトを隠さない)。FTP複数選択DL + 進捗ダイアログ (速度/ETA/キャンセル)。last_dir 自動記憶 (両アプリ)。SSH キープアライブ + 自動再接続。折り返し行のジャンプバグ修正 (`findBlockByNumber`)。Grep ジャンプ時の自動リロード + テキスト一致フォールバック。設定/エクスポート/インポートを Tools → File メニューへ移動。 |
| **1.0.0** | 初版リリース。SSH/Telnet接続、グリッド表示 (1×N / 2×N / 3×N / 4×N の幅広プリセット)、Tail -F + ローテーション追従、SQL/ログ構文ハイライト、テーマ6種 + 設定ダイアログ、ワークスペース、接続プロファイル/ワークスペース管理ダイアログ (ドラッグ並べ替え)、Sora Editor 連携 (.gz自動解凍)、外部編集検知、検索デバウンス、検索/置換履歴永続化、選択語クイック検索、行ジャンプ、編集箇所ナビ、全保存 / 全保存→終了、設定の Export / Import (PC移行支援)、EXE ビルド、起動時クリーンアップ |
